# Repo instructions

- Whenever a branch is merged into `main`, update `README.md` as part of that
  merge (e.g. a dated `## Changelog` entry summarizing what changed, and any
  other sections affected by the change).
