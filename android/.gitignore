*.iml
.gradle
/local.properties
.idea
.DS_Store
/build
*/build
/captures
.externalNativeBuild
.cxx
local.properties

# Synced from the web app's build output; regenerated by the syncWebAssets
# Gradle task, not hand-edited or committed.
app/src/main/assets/www

# Real release-signing secrets (see keystore.properties.example) — never committed.
keystore.properties
*.jks
*.keystore
