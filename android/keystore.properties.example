# Copy this file to keystore.properties (gitignored) and fill in the real
# values to produce a properly-signed release APK locally, e.g. for
# publishing to a GitHub Release. See android/README.md > "Release signing &
# distribution" for how to generate a keystore.
storeFile=/absolute/path/to/release.jks
storePassword=changeme
keyAlias=world-time-release
keyPassword=changeme
